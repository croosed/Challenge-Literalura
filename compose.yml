services:
  postgres:
    image: postgres:16
    container_name: literalura_postgres
    restart: always
    environment:
      POSTGRES_DB: literalura
      POSTGRES_USER: jsaire
      POSTGRES_PASSWORD: 1234
    ports:
      - "5432:5432"
